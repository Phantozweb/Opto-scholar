
// Gemini API features have been removed.
export const summarizeAbstract = async (abstractText: string): Promise<string> => {
    return "";
};

export const explainMedicalTerms = async (abstractText: string): Promise<string> => {
    return "";
};

export const chatWithPaper = async (abstractText: string, userQuestion: string): Promise<string> => {
    return "";
};
